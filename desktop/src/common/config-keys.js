/**
 * Keys of the small preference store this shell keeps. Everything else the
 * node needs lives in the node's own config file, not here.
 */
const CONFIG_KEYS = {
  AUTO_LAUNCH: 'autoLaunch'
}

module.exports = CONFIG_KEYS
