/**
 * The public SDN landing page intentionally owns no credential, private-key,
 * derivation, signing, or wallet-storage implementation. Login and Account
 * are supplied by the pinned, isolated SDN wallet client.
 */

export {};
