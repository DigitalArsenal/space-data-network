- 2026-07-19 · GRAPH: **OWNER-VISIBLE MILESTONE LIVE — rebuild+reship+restart DONE, 7/7 checks PASS** · shipped kubo `b550c091` (binary 493ce56d, backup ipfs.bak-20260719-152655, restart 15:26:56) · (1) config panel GREEN via compat bridge (5 providers, 3h); (2) run log = exactly 1 run: backfill 10,847 objects, eph 10847, avg_rms 0.129 — 82 legacy rows GONE; (3) drill-down serves all 10,847 objects w/ real fit data + omm_cid downloads (ISS 25544: rms 0.0723, 30 iter, 15d span — current NASA arc, ≠ fixture arc, both valid; determinism gate stays on fixture); (4) editor restyle live ($APP re-seeded, new cid); (5) zombies gone from /modules; (6) **CROSS-RESTART PERSISTENCE PROVEN IN PROD** — 10,847 objects visible ~1 min post-boot = snapshot reload (fresh drain would take ~50 min); (7) flow mounted, 3h timer, omm role ACTIVE, daemon healthy. Purge + runs-clear picked up by restart exactly as planned. Rollback = one command. IN FLIGHT: provenance wiring (module node, next rebuild), monorepo push (background), glonass ftp / fetch parallelization / toolchain v3 follow-ups.
