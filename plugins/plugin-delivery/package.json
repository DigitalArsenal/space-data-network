{
  "name": "@spacedatanetwork/plugin-delivery",
  "version": "0.1.0",
  "description": "SDN plugin delivery module — fetches plugins from IPFS and delivers them encrypted via X25519 ECIES",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../build.mjs plugin-delivery",
    "build:clean": "rm -rf .build dist && node ../build.mjs plugin-delivery"
  },
  "keywords": ["sdn", "wasm", "ipfs", "encryption", "x25519", "ecies"],
  "repository": {
    "type": "git",
    "url": "https://github.com/DigitalArsenal/sdn-plugin-delivery"
  }
}
