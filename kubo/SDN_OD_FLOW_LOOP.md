- 2026-07-19 · GRAPH: composition-mechanism SPIKE PROVEN (no blocker) · kubo `09782160` · AOT wasi-threads reactor (4 workers joined, 4 distinct tids) → Go host trampoline read record from REACTOR shared memory → INSERT+SELECT+ExportData→LoadAndRebuild into dedicated-thread flatsqlrt engine under module lock → row count returned; no trap/deadlock, engine never on the composed executor, opaque round-trip stable (30s = one-time engine AOT, cached). REFINEMENT relayed to module node: trampoline can marshal into the engine's HIGH-LEVEL API from reactor memory — store node ABI can be minimal record-in/rows-out (recommended: C++ derives type/table/values, emits opaque statement envelope, single flatsql.exec_envelope import; Go NEVER derives types/tables) instead of the raw malloc/poke engine-memory ABI flow_runtime.cpp assumed. Awaiting module node's declared surface → real wiring (SDN_FLATSQL_LINKED only-if-helpers, cronmount admit, positive wasmImportsModule("flatsql") invariant) → compose → local e2e → guardian → ship.
