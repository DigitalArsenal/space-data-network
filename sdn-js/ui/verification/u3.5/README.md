# U3.5 verification evidence — DATA view (standards + schema explorer)

Captured 2026-07-11 by the coordinator against a locally built daemon
(isolated scratch home) at `http://127.0.0.1:15080/console/data`, live
admin session.

## Files

- `console-data-reference-1440x900.png` / 
  `console-data-generate-reference-1440x900.png` — the mock's DATA
  route (EXPLORER and GENERATE tabs, fixture data).
- `console-data-port-real-1440x900.png` — the port: real banner
  (`v1.136.0` from `node/info.standards_version`, `173 standards` from
  `/api/v1/channels`), rows-first sort (PRR 6 rows, EPM 2 rows), EPM
  auto-selected with its full schema-exact field table.
- `console-data-explorer-epm-1440x900.png` — EXPLORER: FIELD/TYPE/NOTES
  parsed client-side from the vendored `main.fbs` (DN, LEGAL_NAME, …,
  `[CryptoKey]` vector types, doc-comment notes).
- `console-data-generate-omm-1440x900.png` — GENERATE: 8 target
  languages, `OMM_generated.ts`, honest "generated client-side" header
  (the mock's "Generated by flatc" would be false), ↓ .fbs +
  ↓ DOWNLOAD CODE.

## Acceptance highlights

- **IDL byte-for-byte (≥2 standards)**: rendered IDL block vs the
  pinned vendored file, live in the DOM — CAT: 2464 chars, FNV-1a
  `75b7a9b1` (identical both sides); OMM: 4367 chars, `a3a4c96c`
  (identical). Also asserted in unit tests.
- **No `/api/v1/standards`**: the endpoint is planned-only (docs.go
  `plannedIn: "G.2"`; authenticated fetch 404s) — recorded server gap.
  The standards list joins `/api/v1/channels` (public; one entry per
  code) with `stats.schemas[]` row counts (`PRR.fbs` ↔ `PRR`).
- Honesty: no padlocks rendered (every live channel reports
  `encryptionState:"none"`; the 🔒 logic is unit-tested against a
  synthetic encrypted fixture); standards missing from stats show a
  verified "0 rows" (server only emits entries with count > 0);
  non-vendored live codes (e.g. PRR — channels can exceed the 169
  vendored schemas) degrade to an honest "no vendored schema" message;
  DATA tab and MODULES toggle render honest U3.6 placeholders.
- Coordinator fix: default selection prefers the first standard WITH a
  vendored schema (rows-first sort put non-vendored PRR on top, making
  the view's first render a "no schema" message); the left-list
  highlight follows the effective selection.
- Artifact grew 5578 → 6137 KiB (+0.55 MiB) from embedding all 169
  vendored `main.fbs` files (896 KB raw) — everything stays inline.

## Audits

- Console: completely clean on a fresh load.
- Network: exactly 6 requests, all same-origin — document, `auth/me`,
  `data/health`, `v1/channels`, `v1/stats`, `node/info`. Zero external;
  zero runtime schema fetches (all IDL inlined at build time).
- No document scroll at 1440×900 (left standards list scrolls
  internally).
