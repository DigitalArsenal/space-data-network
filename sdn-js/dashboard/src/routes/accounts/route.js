/**
 * ACCOUNTS — operator keys and the peer trust registry.
 *
 * ACCOUNTS inherits ⬡, the template's THIRD rail glyph
 * (`SDN Console.dc.html:892`: `['groups','GROUPS','⬡','N3']`), freed by the death
 * of THIS NODE (owner directive 2026-07-30: "the 'this node' menu should go
 * away"). Nothing was lost with it: every fact its widgets carried — xpub,
 * derivation paths, signing/encryption keys, EPM signature/timestamp/CID, chain
 * addresses, multiaddrs, trust, the vCard and the QR — is in the node detail
 * modal that every OTHER peer already gets, and the self node is a node (IRIS
 * R6). The IDENTITY widget's DETAIL button opens it.
 */
export const id = 'accounts';
export const label = 'ACCOUNTS';
export const glyph = '⬡';
export const section = 'NETWORK';
export const title = 'ACCOUNTS';
/** A ROUTE IS ITS NAME (owner directive 2026-07-30, twice). */
export const sub = '';
export const order = 3;
