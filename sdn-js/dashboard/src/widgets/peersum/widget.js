/** PEER SUMMARY — the design's `SDN Console.dc.html:869` entry (add-only). */
export const id = 'peersum';
export const title = 'PEER SUMMARY';
export const spans = [4, 6];
export const def = 4;
export const order = 6;
/** Plots the public feed's peer rows; no admin snapshot. */
export const privileged = false;
/* Add-only: in neither default layout, present in the ADD tray for both. */
