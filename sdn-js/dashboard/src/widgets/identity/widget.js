/** IDENTITY — the design's `SDN Console.dc.html:864` entry. */
export const id = 'identity';
export const title = 'IDENTITY';
export const spans = [4, 6];
export const def = 4;
export const order = 2;
/** Renders entirely from the public feed's vCard; no admin snapshot needed. */
export const privileged = false;
export const defaultSpan = 4;
/** 6 on the anonymous grid, for the same tiling reason NODE HEALTH takes 6. */
export const publicSpan = 6;
